<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingfenglong.ssm.mapper.JobMapper">
    <!-- Job getJobById(@Param("jobId") Integer jobId); -->
    <select id="getJobById" resultType="Job">
        select * from jobs where job_id = #{jobId}
    </select>
</mapper>
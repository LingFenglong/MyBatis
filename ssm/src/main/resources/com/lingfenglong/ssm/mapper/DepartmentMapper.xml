<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingfenglong.ssm.mapper.DepartmentMapper">
    <!-- Department getDepartmentById(@Param("departmentId") Integer departmentId); -->
    <select id="getDepartmentById" resultType="Department">
        select * from departments where department_id = #{departmentId}
    </select>
</mapper>